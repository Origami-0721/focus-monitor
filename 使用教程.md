# 使用教程

> **这份教程和 README 的分工**
> README 回答「它是什么、为什么这么设计」；本教程只回答**「我该怎么用」**——
> 从第一次双击到两周后决定要不要调阈值，按时间顺序走一遍。
>
> 想查参数含义、隐私边界、设计取舍，回 [README](README.md)。

---

## 目录

1. [第 0 步：跑起来](#第-0-步跑起来)
2. [第一天：让它先学会你的脸](#第一天让它先学会你的脸)
3. [日常：三个入口](#日常三个入口)
4. [怎么读报告](#怎么读报告)
5. [打分：唯一能验证数据准不准的事](#打分唯一能验证数据准不准的事)
6. [两周后：决定要不要调阈值](#两周后决定要不要调阈值)
7. [每月一次：备份和清理](#每月一次备份和清理)
8. [出问题了怎么查](#出问题了怎么查)
9. [参数速查](#参数速查)

---

## 第 0 步：跑起来

### 如果你下载的是 exe（推荐）

1. 解压 zip，得到 `focus-monitor` 文件夹
2. 双击里面的 **`focus-monitor.exe`**
3. 首次运行会自动下载两个模型（约 11MB），**要等十几秒**，期间没反应是正常的
   —— 托盘会冒个气泡说「正在准备」，不用干等着猜
4. **主窗口不会自己弹出来**（默认如此，见下面「启动时要不要自动弹面板」）。
   想看就从托盘图标点一下，或者右键 →「显示面板」

> 早期版本首次运行会**自动弹窗**，本意是"托盘图标藏在 `^` 折叠区里，
> 不弹窗你会以为双击了没反应"。但那个窗口**每次启动都弹**，很打扰人，
> 所以现在默认不弹了 —— 想知道"到底跑起来没有"，看桌面开关的**绿点**
> 更准（绿点只在进程真的起来之后才写）。

数据（`focus.db`、`focus.log`、报告、CSV）都存在 **exe 所在的文件夹**里。
所以整个文件夹可以拷到 U 盘或另一台电脑，数据跟着走。

> **别把 exe 单独拖出来**。只拷 exe 会把数据留在原地，看起来像「记录丢了」。

### 如果你从源码跑

```powershell
uv run focus.py --selftest     # 先自检：不开摄像头，1 秒出结果
uv run focus.py                # 开始监视
```

`--selftest` 通过就说明代码没问题，之后遇到任何异常都**不用怀疑是装错了**，
直接跳到[出问题了怎么查](#出问题了怎么查)。

### 建议第一次就做的两件事

```powershell
uv run focus.py --install-startup    # 开机自启（这样才不会忘记开）
uv run focus.py --install-shortcut   # 桌面开关（绿=在记录，黄叹号=没在记录，灰杠=已停）
```

---

## 第一天：让它先学会你的脸

**这一天的数据不要拿去下结论。** 头一两周都是校准期。

原因是眼睛闭合判定（EAR）默认值取自「大多数人」，而 EAR 跟脸型强相关 ——
同一套硬件默认值，在别人脸上是「睁眼」，在你脸上可能就成「闭眼」了。

好消息是**它会自己学**，你不用做任何事：

- 程序滚动保留你最近一段时间的 EAR，取 **75 分位**当「你的睁眼基线」
- 阈值 = 基线 × 0.67
- 攒够 **60** 个样本才启用

进度直接写在主窗口上：

```
睁眼基线 0.312 · 已自适应        ← 学好了，可以信疲劳判定了
睁眼基线学习中 37/60              ← 还没攒够，再等等
```

**看到「睁眼基线学习中」是正常的，不是坏了。** 这段时间疲劳判定可能偏松或偏严，忽略即可。
（界面上**不会**出现"校准"两个字 —— 没有校准这个操作，见下面那节。）

### 第一天该做的

1. **正常用电脑**，不用刻意配合 —— 基线靠的是自然使用
2. 顺手做一次下面的[打分](#打分唯一能验证数据准不准的事)，熟悉一下流程
3. 第二天打开报告，先只看**「应用使用记录」**这一块

**为什么先只看应用记录**：它不依赖摄像头，纯粹是前台窗口统计，
所以第一天就已经是准的。拿它验证「程序有没有在正常记录」最快 ——
如果你今天明明用了一下午电脑，这里却只有几行，那就是没在记录，
直接去[出问题了怎么查](#出问题了怎么查)，别继续往下看其他数字。

---

## 日常：三个入口

### 1. 托盘图标（主入口）

图标在任务栏的 **`^` 折叠区**里 —— 找不到就先点那个 `^`。
想让它一直可见，可以把它从折叠区拖到任务栏上（Windows 会把该图标设为常显）。

**图标用「颜色 + 形状」两条通道表示状态**，所以眯一眼就能知道现在是什么：

| 图标 | 状态 | 颜色 |
|---|---|---|
| ● 实心圆 | 专注 | 绿 |
| ◐ 半环 | 中性 | 蓝 |
| ▬ 横杠 | 伏案 | 青 |
| ▲ 三角 | 走神 | 橙 |
| ○ 空心环 | 疲劳 | 紫 |
| – 短横 | 离开 | 灰 |
| **! 感叹号** | **记录停了** | **橙** |

把鼠标停在图标上会显示同样的文字（如「专注监视 — 专注」）。

> **那个感叹号是最重要的一格。** 线程一旦死掉，状态就会**冻在最后一个值上** ——
> 如果那恰好是「中性」，你看到的会是一个和健康状态**一模一样**的蓝点。
> 所以程序会在收不到新样本时把图标换成感叹号，标题写「无数据（可能已停止记录）」。
> 看到它就说明采集真的停了，去[出问题了怎么查](#出问题了怎么查)。

| 操作 | 效果 |
|---|---|
| **左键单击** | 有待评分 → 直接打开评分页；否则 → 打开主窗口 |
| **右键** | 菜单（见下） |

右键菜单最上面两行是**只读状态**，跟着实时数据走，不用打开窗口就能瞄一眼：

```
当前状态：专注
今日投入：3 小时 12 分
─────────────────
打开主窗口（左键点我）
自述评分
完整报告
暂停          ← 点一下变成「继续」
打开日志
在桌面建开关快捷方式
退出
```

**「暂停」和「退出」的区别**：
- **暂停**：停止记录，但程序还在（图标还在）。适合临时不想被记录的时候。
- **退出**：程序结束，图标消失。

> **系统气泡点了没反应是正常的**。Windows 的托盘气泡需要单独处理点击事件，
> 这个程序没接。所以提醒文案写的是「点托盘图标」，那才是可靠路径。

### 2. 桌面快捷方式（开关）

双击即**切换开/关**，不用右键翻菜单。图标会跟着程序**真实的运行状态**变：

| 图标 | 状态 | 含义 |
|---|---|---|
| 🟢 绿圆 | 正在记录 | 程序在跑，采集也正常 |
| 🟡 黄底感叹号 | 在跑，但没在记录 | 采集线程停了（模型没下完 / 摄像头被占 / 刚休眠唤醒） |
| ⬛ 灰底横杠 | 已停止 | 程序没在跑 |

> **图标是程序自己刷的，不是"你按过开关"的纪念品。** 早先的版本是双击时
> 先写成绿点、再去起进程，起没起来都不再看一眼 —— 断网或长时间待机之后
> 模型下不下来、采集根本没起，桌面却一直是绿的，你以为在记录，其实一条
> 样本都没有。现在绿点只在**确认进程真的起来**之后才写；起不来会置灰并
> 弹窗告诉你原因。

**怎么建出来**：

- **exe 用户**：托盘右键 →「在桌面建开关快捷方式」。
- **源码运行**：`uv run focus.py --install-shortcut`。

> **别用「右键 exe → 发送到桌面快捷方式」**。那样建出来的快捷方式没有
> `--toggle` 参数，双击只会去起第二个实例、被单例挡住 —— 看起来像"点了没反应"。
> 开关必须由程序自己生成，它才知道该指向 exe 还是 pythonw。

### 3. 主窗口

面板、评分、报告都在**程序自己的窗口**里（用系统自带的 WebView2 渲染，
不占用浏览器，跟随系统深浅色主题）。

- 点窗口的 **×** = 最小化到托盘（**不是退出**）
- 窗口里的 **设置** 页可以改所有参数，**保存后 1 秒内生效，不用重启**

窗口里的实时面板每 3 秒刷新一次动态区域，显示当前状态、今日投入、
睁眼基线的学习进度。服务只监听 `127.0.0.1`（默认端口 8787，被占用会自动
往后顺延），不对外网开放。

#### 启动时要不要自动弹面板

设置页 →「行为」→「启动时自动打开面板」，**默认不勾**。

不勾的时候，双击桌面开关只会起进程，**不会弹任何窗口** —— 想确认它活了就看
绿点。要看面板点托盘图标（那一下不受这个开关影响：它管的是"自动"，不是
"能不能看"）。

勾上之后，启动时面板会自己打开（模型要下十几秒，所以是等就绪之后才弹）。
首次运行也一样受它管 —— 所以想给第一次用的自己留个引导，就先把这一项勾上。

---

## 怎么读报告

```powershell
uv run focus.py --report      # 生成 HTML 报告并打开
```

或者托盘右键 →「完整报告」。同时会在同目录写一份 `focus-日期.csv`
（UTF-8 BOM + 表头，Excel 直接双击就能打开）。

### 六种状态的含义

| 状态 | 含义 | 算「投入」吗 |
|---|---|---|
| **专注** | 看着屏幕 + 工作类应用 | ✅ |
| **中性** | 看着屏幕 + 其他应用（含娱乐应用、不认识的） | ✅ |
| **伏案** | 低头对桌子（看书/写作业） | ✅（可在设置里关掉） |
| **走神** | 没在看屏幕（转头/低头过狠），或一分钟里来回切了太多次窗口 | ❌ |
| **疲劳** | 持续闭眼 | ❌ |
| **离开** | 键鼠空闲 / 人脸消失超时 | ❌ |

**「中性」算投入是刻意的**。查资料、翻文件、写文档都会落在这里，
不算的话你会觉得「记录少了一大块」。

> **应用类型永远不会单独判你走神。** 早期版本会按标题关键词直接判，
> 于是「在 B 站看半小时高数课」被记成走神（实测就是这么被投诉的）。
> 平台是娱乐的、内容不是，而程序只看得到进程名和标题、看不到内容，
> 所以它没资格下这个结论。
>
> 现在娱乐应用只降到**中性**（中性算投入时长，不扣分），
> 走神只剩两条行为判据：**没在看屏幕**，和**来回切窗口**
> （一分钟超过 `DISTRACT_SWITCH_RATE` 次，默认 20；觉得抓不住就调小）。

### 眼睛该歇会儿了 —— 注意：这不是「疲劳」

除了上面那六种状态，还有一条**独立的提醒**：连续用眼太久会弹一个气泡
（面板上同时出现一条横幅）。它**不是第七种状态**，两者别混：

| | 判什么 | 怎么表现 |
|---|---|---|
| **疲劳** | 你**困了**（持续闭眼） | 状态变成「疲劳」，不算投入 |
| **眼睛该歇会儿了** | 你**盯屏幕太久了**（视疲劳/干眼） | 弹一次气泡，状态**不变** |

**为什么不把它做成状态。** 状态描述的是「我在干什么」，而「眼睛该休息」
是关于身体负荷的**建议**。混进状态的话，这段时长既不算投入也不算走神，
你的投入统计会被污染 —— 而它本来只是想说一句「抬头看看远处」。

**什么时候提醒：**

- 连续用眼满 **40 分钟**（默认）→ 提醒一次；
- 如果这期间**眨眼率低于 8 次/分**，提前到 **25 分钟**就提醒；
- 两次提醒至少隔 **20 分钟**，别念烦你；
- 真的离开屏幕满 **5 分钟**才算休息过，用眼时长从零开始
  （起身倒杯水不会重置，否则永远攒不到 40 分钟）。

这四个数都在**设置页 → 视疲劳提醒**里，按自己习惯调。

**嫌那个气泡吵就关掉它**：设置页 →「行为」→「眼睛该歇会儿了」，取消勾选即可。
关掉之后**只是不弹气泡** —— 面板里那条横幅照旧（它只在你自己打开面板时可见，
不打扰人），日志里也照旧写「视疲劳提醒：…」。判定本身不受影响，所以"没提醒"
照样分得清是眼睛不累、还是这条链路坏了。

**眨眼是怎么数的**（知道它怎么算，才知道什么时候该不信它）：

- 眼睛闭合一下又睁开算一次；**闭得超过 0.6 秒不算眨眼**——那是眯眼或
  打瞌睡，归「疲劳」那条管。不排除的话会变成「越困眨眼越多」，方向正好反了；
- 只有**正对屏幕**时完成的眨眼才计入。转头时眼睛在画面里被压扁、EAR
  假性变低，不排除掉眨眼率会凭空翻几倍；
- 眨眼率的分母是**窗口里真正观察到正脸的秒数**，不是窗口长度。所以走开
  一阵再回来不会被算成「十秒里眨了 0 次」而误报。

面板上**一直显示**「眨眼 X 次/分 · 最近一分钟揉眼 N 次 · 本轮连续用眼 X」。
留着它是为了让你能自己确认这套检测在不在工作 —— 只在你该休息时才显示的话，
「没提醒」到底是「眼睛不累」还是「检测坏了」就分不出来了。

**揉眼只记录，不参与判定。** 摄像头用的是 MediaPipe 的 Pose 模型，它只给
33 个身体点，**没有手指**，只有手腕。所以「手在眼周」这个判断分不开
托腮、扶眼镜、挠头、喝水 —— 直接拿去判疲劳会大面积误报。先只把次数记进库，
攒够真实数据再决定要不要用它，跟当初标定走神阈值是同一个做法。

**记录下来的东西在哪看。** 报告页最下面「身体信号与坐姿」一节里多了一块
**「视疲劳（眨眼 / 揉眼）」**：眨眼率中位数、「眨眼偏低」的占比、揉眼次数。
那一节是**纯描述**的，不含任何判定，和采集端一致。

> **两列的数据要等重启一次才会开始有。** 它们是新加的列，`ALTER TABLE`
> 只在采集循环启动时执行 —— 所以打开报告时如果看到「还没开始记录」，
> 说明应用还在跑旧版本，**关掉再开一次**就有了。
>
> **刚重启完的那阵子，那两张卡片可能显示「—」。** 眨眼率要"窗口里至少有
> 30 秒正对屏幕"才数得出来，在那之前显示的是「还不知道」——
> 「眨眼偏低的占比」那一格也一样显示「—」。**「—」和「0%」是两回事**：
> 前者是"还没测出来"，后者才是"测了，而且很正常"。别把「—」读成"我很好"。
>
> 另外：库里 `ear` 那一列存的是每秒 10 帧的**中位数**，0.1~0.4 秒的眨眼
> 早就被抹平了 —— 所以**拿历史 `ear` 回放永远数不出眨眼**。
> 报告里的眨眼率是采集时在**帧级**数据上实时算出来的，两者不是一回事。

### 建议的阅读顺序

1. **应用使用记录** —— 列出**所有**用过的应用（含时长、占活跃比、出现时段、主要状态）。
   先看这里，确认数据是不是符合你的印象。
2. **近 14 天每日投入** —— 每天一根柱，悬停看当天投入时长和专注率。看趋势，不看单日。
3. **黄金时段** —— 你最想问的那个问题。**只看累积满 10 分钟的钟点**，
   所以不会出现「某天 3 秒 100% 专注」这种假排名。
4. **全天活跃分布** —— 按「平均每天」归一化。只有部分天用电脑的钟点不会被撑虚高，
   看的是**作息节律**而不是总量。
5. **走神拆解** —— 把走神分成「应用导致」和「人没看屏幕」两部分。
6. **视疲劳（在「身体信号与坐姿」里）** —— 眨眼率中位数和揉眼次数。
   放在最后是有意的：这两个数**只看趋势，不看单日**，而且揉眼必然会多报
   （手腕近似，分不开托腮/扶眼镜）。它的用处是"这周是不是比上周眨眼更少了"。

### 第 5 项为什么重要

这两种走神的**解法是相反的**：

- **应用导致的** → 解法是屏蔽/关掉那个应用（现在指的是"你在这几个页面之间
  来回切"，而不是"你打开了某个应用"）
- **人没看屏幕**（转头、低头）→ 解法是休息或换任务

混在一起统计，你会得出「快速设置是你最大的分心源」这种荒谬结论 ——
实际上你只是扭头跟人说了句话。

---

## 打分：唯一能验证数据准不准的事

这个程序只会输出数字，**没有任何东西能证明那些数字和你的真实感受对得上**。
所以有一层自述评分。

### 怎么打

托盘右键 →「自述评分」，或左键单击（有待评分时）。每 **30 分钟一块**，
凭感觉打 **1–5 分**。

> **评分界面不显示任何实测数据，这是硬约束。**
> 要是写着「这段测出 85% 专注」，你会被那个数字锚定，
> 收集到的就不是独立评价，没法用来验证任何东西。

### 什么时候会提醒你

**只在自然断点**（走神 / 离开 / 疲劳）。如果你正处在连续投入里（≥5 分钟），
提醒会自动推迟 —— 在人专注正盛的时候弹窗是本末倒置。

但「不打扰」有上限：**连续投入超过 2 小时仍会提醒一次**。

这不是随便定的。纯等断点会造成一个反直觉的偏差：一整个上午连续投入、
只在切窗口那几十秒断一下的人，几乎没有断点，于是**最专注的那些时段一个评分都收不到** ——
样本被系统性地偏向前半天被打断的时段。而相关性验证的恰恰就是「专注」，
这个偏差方向最坏。托盘气泡不抢焦点、不阻塞输入，比「永远收不到训练数据」划算。

**嫌那个气泡吵就关掉它**：设置页 →「行为」→「到点提醒打分」，取消勾选即可。
关掉之后**只是不弹气泡** —— 待评时段照样在攒，点托盘图标照样进评分页，
面板和报告也照常。攒着不打也不会丢。

（它不管「眼睛该歇会儿了」那条提醒 —— 那条有自己的开关，见上面那节。）

### 打多少才够

**至少 30 对**才有结论。门槛定这么高不是保守：拿纯随机数据跑 3000 次模拟，
**8 对**样本下「|r| ≥ 0.4」有 **30.8%** 的概率是假阳性。八个点算出来的
「中等相关」，三次里有一次纯属噪声。

所以：**前 30 个评分只是攒样本，别急着看结论。**

---

## 两周后：决定要不要调阈值

两周后报告里会出现**相关系数**（测出来的 vs 你感受到的）：

| 相关系数 | 含义 | 该做什么 |
|---|---|---|
| **≥ 0.7** | 测出来的和感受到的是一回事 | **数据可信，别动阈值** |
| **0.4 – 0.7** | 大方向对得上 | 阈值还有调的空间 |
| **< 0.4** | 系统性对不上 | **看阈值 —— 但样本还少的时候先别动手** |

> **样本刚好过 30 对时别急着调。** 这时置信区间还很宽（n=30、r=0.6 时约 ±0.34），
> 相关系数随时会变。报告里那句结论后面会跟着提醒：样本少的时候写的是
> "先别急着动阈值，再攒一些评分看看"，样本够了才会直接说阈值要调。
> **先攒到 50–60 对再动手**，否则你很可能是在把本来正常的判定改坏。

### 怎么调

**顺序：先动 EAR，再动姿态角。** EAR 影响最大也最好理解。

打开主窗口 → **设置**。改完保存，**1 秒内生效，不用重启**。

要改的具体是哪几项，就在设置页 **「判定阈值」** 那一组里：
**闭眼 EAR 阈值**、**持续闭眼判疲劳（秒）**、**头部左右偏容差（度）**、
**抬头/低头容差（度）**。

> **找不到「校准」在哪？**
> 因为程序里**没有**单独的校准功能 —— "校准"是个**时期**（头一两周），
> 不是一个界面。EAR 基线是自动学的，你不需要操作。
> 上面说的"调阈值"就是改设置页里那几项，**没有别的地方**。
>
> 报告页顶部那条「先别信其他结论」的横幅如果让你去调阈值，
> 现在也会直接写出位置；报告页本身也加了导航，点「设置」就过去了。

### 两条硬规矩

- **不要为了「好看」调高阈值**。把专注判松了，报告数字漂亮了，但数据就废了
- **不要照着别人的配置抄**。阈值是跟你的脸、摄像头位置、光照绑的，抄过去只会更差

### 一个容易踩的坑

**状态是在采集时就写进数据库的。**

所以改完工作应用 / 娱乐关键词 / 学习豁免这几张表，**要重新记录一段时间才生效** ——
已经记下的历史不会被追溯修改。别改完立刻去看报告，然后以为「改了没用」。

顺带说清一件事：**娱乐应用关键词不再产生走神**。命中它只是"不硬说专注"
（降到中性，中性算投入时长）。想让某个应用完全不参与判定，把那一栏清空即可。

---

## 每月一次：备份和清理

样本大约 **1 秒 1 行**，一年下来 0.5–1 GB，而且只增不减。

### 备份

```powershell
uv run focus.py --backup                        # → focus-backup-20260916-132911.db
uv run focus.py --backup --out D:\my-backup.db  # 指定位置（比如移动硬盘）
```

**监视正在跑的时候也能安全执行**，不用先退出。

> 用的是 SQLite 的在线备份接口，不是复制文件。WAL 模式下 `focus.db` 和
> `focus.db-wal` 是两个文件，**单拷 focus.db 会安静地丢掉最近一段还没合并的样本**
> —— 库能打开、能查，只是少一截，最难发现的那种坏法。

### 清理

```powershell
uv run focus.py --compact                       # 预演：只告诉你「能删多少」，不删
uv run focus.py --compact --days 180 --yes      # 真删 180 天以前，并回收空间
```

`--compact` 是**唯一会删数据的操作**，所以设了三道闸门：

1. **默认只预演**。不加 `--yes` 就只打印「要删多少行、什么时间范围」，然后结束
2. **删之前强制先备份**。备份失败就中止，绝不允许「删了但没备份」
3. 备份路径和删除范围都会打印出来，随时能自己找回

> **删掉的样本不会被聚合成摘要，报告里那段历史就没有了。**
> 这是刻意的取舍 —— 报告里每个数字都来自同一条样本序列，把一小时压成一行之后
> 那行只能贡献 5 秒，所有时长统计都会塌掉。
> **想全留着就别执行 `--compact`。**

### 日志

`focus.log`，最大 1 MB、保留 3 个滚动备份。托盘右键 →「打开日志」直接看。

---

## 出问题了怎么查

按这个顺序查，绝大多数问题在前两步就能解决。

### 1. 图标不见了

先点任务栏的 **`^` 折叠区**。图标默认藏在那里 —— 这是最常见的「没反应」。

**图标在、但想知道有没有在记录**：把鼠标停上去看标题。
显示「专注监视 — 专注」之类的是正常的；
显示 **`! 无数据（可能已停止记录）`** 就说明采集停了 ——
这时程序还在、托盘还在响应，但已经没有新样本进来了。

还是没有图标 → 托盘右键 →「打开日志」，看 `focus.log` 最后几行。

### 2. 提示「已经有一个实例在跑了」

程序本来就只允许跑一个（防止两份数据同时写同一个库）。
要重启就先退出：托盘右键 →「退出」，或者

```powershell
uv run focus.py --stop
```

### 3. 记录不全 / 报告里某个时段是空的

**先分清是「没记录」还是「记录成别的状态」**：

| 现象 | 原因 |
|---|---|
| 托盘图标是 **`!` 感叹号** | 采集线程停了 —— 这是最明确的信号，先看日志 |
| 桌面快捷方式是 **黄底感叹号** | 同上，程序在跑但没在记录 |
| 整个时段都没有行 | 程序没在跑（图标不见了），或当时在「暂停」 |
| 有行但全是「离开」 | 人脸没被检测到（摄像头角度/光照），或键鼠真的空闲 |
| 有行但算成「中性」 | 用的应用不在工作应用表里，也不匹配任何关键词 |
| 某天整段缺失 | 那天没用电脑，或程序没启动 |

> **刚睡醒（休眠/待机唤醒）或断网之后，先看一眼桌面开关的颜色。**
> 这两种情况最容易让采集悄悄停掉：摄像头在唤醒后可能打不开，
> 模型也可能因为断网没下完。黄叹号 = 程序在、但没在记录，双击一下关掉、
> 再双击开启，通常会自己恢复。

**第 3 行是最常见的**。默认列表覆盖 VS Code、JetBrains、Office、终端、
浏览器（配合关键词）等，但**游戏和冷门软件不会被认出来**，
一律静默归到「中性」。

修法：主窗口 → 设置 → 把该应用的 exe 名加进工作应用表。
**注意改完要重新记录才生效**（分类在采集时就写进库了）。

### 4. 摄像头不通 / 画面是黑的

```powershell
uv run focus.py --camera 1     # 换一个摄像头序号试试
```

笔记本外接显示器时，摄像头序号常会变。默认是自动挑，自动挑错了就手动指定。

### 5. 数字看着不对劲

**先看相关性，别直接改阈值**：

- 评分数**少于 30 对** → 样本不够，还没有结论，继续打分
- 相关性 **< 0.4**、且样本已经攒到 50–60 对 → 系统性对不上，按[两周后](#两周后决定要不要调阈值)那节调
- 相关性 **< 0.4**、但样本才刚过 30 对 → 先别动手，结论还可能变，继续打分
- 相关性 **≥ 0.7** → 测量是可信的，那问题在别处（比如应用分类）

### 6. 面板打不开

面板是**附属功能**，它挂了不影响采集 —— 数据还在正常记录，不用担心。

面板只监听 `127.0.0.1`。默认端口 8787，被占了会**自动往后顺延**
（最多试 20 个），真实地址看 `focus.log` 里那行「实时面板: http://…」。

**点托盘菜单没反应，或者页面开在了浏览器里而不是应用窗口** ——
那是窗口层（pywebview / WebView2）没加载起来。程序会**自动退到系统浏览器**，
页面一样能看，采集和托盘都不受影响。补上依赖即可：

```powershell
uv sync          # 按 pyproject.toml 装齐依赖（含 pywebview）
```

> **从旧版本更新上来最容易撞到这条。** 老版本是用浏览器开面板的，
> 压根不需要 pywebview；更新后如果没重新 `uv sync`，窗口层就加载不了。
> 以前这种情况会把**整个程序带走**（启动两秒后进程就没了，托盘图标一闪），
> 现在只是降级成浏览器打开 —— 记录照常。

**在窗口里点「提交」，跳出来的却是一张 403 页面** —— 那是本地面板的来源
校验在拦（防的是"你浏览器里随便一个网页偷偷改你的设置、或者读走你几个月的
窗口标题"）。早期版本把 WebView2 的表单提交误判成跨源了，于是窗口里的
自述评分、跳过、设置保存**全都点不动**，现在已修好。

万一还遇到，`focus.log` 里会有一行 `拒绝 POST /rate：…`，把
`Host` / `Origin` / `Sec-Fetch-Site` 的值原样记着 —— 把那行发出来就能定位，
不用再靠截图猜。

### 7. 崩溃会丢多少数据

最多丢**几十秒**。样本是攒 **15 条**才提交一次（约 30 秒），
这是刻意的取舍：每条单独提交会让面板轮询频繁撞写锁。

### 8. 确认代码本身没问题

```powershell
uv run focus.py --selftest
```

跑通了就说明逻辑没问题，上面那些现象都是**配置或环境**造成的，
不用重装。

---

## 参数速查

### 命令行

| 命令 | 作用 |
|---|---|
| `focus.py` | 开始监视（托盘常驻） |
| `focus.py --dashboard` | 只起面板服务 |
| `focus.py --report` | 生成 HTML 报告并打开（同时导出 CSV） |
| `focus.py --stop` | 停掉正在运行的实例 |
| `focus.py --toggle` | 切换开/关（桌面快捷方式用） |
| `focus.py --camera N` | 指定摄像头序号，默认自动挑 |
| `focus.py --install-startup` | 装开机自启 |
| `focus.py --uninstall-startup` | 卸载开机自启 |
| `focus.py --install-shortcut` | 装桌面开关快捷方式 |
| `focus.py --backup [--out PATH]` | 备份数据库 |
| `focus.py --compact [--days N] [--yes]` | 清理旧样本（默认 90 天，默认只预演） |
| `focus.py --selftest` | 跑自检，不开摄像头 |
| `focus.py --version` | 显示版本号 |

### 设置页可改的项

| 参数 | 默认 | 作用 / 调它会发生什么 |
|---|---|---|
| `YAW_TOL` | 25.0° | 头部左右偏超过这个角度 → 没在看屏幕 |
| `PITCH_TOL` | 30.0° | 低头超过这个角度 → 走神。**必须小于 `DESK_PITCH_MAX`** |
| `DESK_PITCH_MAX` | 65.0° | 介于两者之间 → 伏案；超过 → 判走神 |
| `EAR_CLOSED` | 0.19 | 闭眼兜底阈值。**基线建好后基本不用动这个** |
| `EAR_SUSTAIN` | 3.0s | 持续闭眼多少秒算疲劳 |
| `AWAY_FACE` | 20.0s | 人脸消失多少秒算离开 |
| `AWAY_IDLE` | 180.0s | 键鼠无操作多少秒算离开。**要大于 `AWAY_FACE`** |
| `FACE_FPS` | 10 | 人脸检测频率，越高越吃 CPU |
| `POSE_FPS` | 2 | 姿态检测频率，动作慢，2Hz 够 |
| `PROC_WIDTH` | 480 | 送进模型的画面宽度，越小越快 |
| `TILT_WARN` | 12.0° | 肩线倾斜超过多少度算坐姿不良 |
| `AWAY_WRITE_EVERY` | 30.0s | 离开期间每 30 秒才落一条（否则整夜待机撑爆数据库） |
| `DISTRACT_SWITCH_RATE` | 20 次/分 | 一分钟里换这么多次前台窗口 → 走神（不看是哪个应用）。调到 9999 等于关掉 |
| `EYE_BREAK_AFTER` | 2400s（40 分） | 连续用眼多久提醒休息（视疲劳，**不是**「疲劳」状态） |
| `EYE_BREAK_SOON` | 1500s（25 分） | 眨眼率偏低时提前到这一档。**必须不大于 `EYE_BREAK_AFTER`** |
| `BLINK_LOW_RATE` | 8 次/分 | 低于此值算「盯屏幕太久」。屏幕阅读常态是 5~7 |
| `EYE_REMIND_EVERY` | 1200s（20 分） | 两次提醒的最小间隔，别念烦你 |
| `DESKWORK_IS_ENGAGED` | 开 | 伏案算不算投入时长。**发现伏案时长虚高就关掉** |
| `AUTO_OPEN_PANEL` | 关 | 启动时自不自动弹面板窗口。**嫌弹窗打扰就保持关着**；想看面板点托盘图标，那一下不受它管 |
| `RATE_REMIND` | 开 | 到点弹「该打个分了」的托盘气泡。**嫌吵就关掉** —— 只是不弹气泡，待评时段照样攒、点托盘图标照样能打 |
| `EYE_BREAK_REMIND` | 开 | 到点弹「眼睛该歇会儿了」的托盘气泡。**嫌吵就关掉** —— 只是不弹气泡，面板里那条提示和日志里的记录都照旧 |
| `WORK_APPS` | 见代码 | 工作应用 exe 名单。命中 → 专注 |
| `DISTRACT_KEYWORDS` | 见代码 | 娱乐应用关键词（含中文标题匹配）。命中 → 只是**不硬说专注**（降到中性），**不再产生走神** |
| `STUDY_KEYWORDS` | 见代码 | 学习豁免词：平台是娱乐的、内容不是（如「B 站看 C++ 课」）。命中 → 直接判专注 |

设置页会校验明显配错的组合（比如俯仰阈值 ≥ 伏案上限 → 永远判不出「伏案」），
配错会直接拒绝保存并说明原因。

---

## 一句话总结

**第一周别看数字，只管正常用电脑 + 打分。**
两周后相关性 ≥ 0.7，你才有资格相信报告里的结论；
低于 0.4，先看样本够不够（刚过 30 对就再攒攒），够了才去调阈值，别急着下结论。
