# ── 个人数据，绝对不要提交 ──
# focus.db 里存着你的窗口标题（聊天、网站、游戏名），是实打实的个人数据。
focus.db
focus.db-*
config.json
focus.log
focus.log.*
focus.pid

# --backup 产出的备份：内容和 focus.db 一模一样，同样敏感。
# 上面那两条通配（focus.db / focus.db-*）**盖不住** focus-backup-*.db —— 
# 名字里没有 "focus.db" 这个片段，很容易以为已经排除了。
focus-backup-*.db*

# 生成的报告同样含个人统计
report.html
demo-report.html

# CSV 导出：逐秒含状态 / 应用名 / 原始窗口标题，和 focus.db 同级敏感。
# 每次生成报告都会自动写出，不排除的话一次 git add -A 就全进仓库了。
focus-*.csv

# 自检用的临时库。正常路径会删掉，中途崩了会残留。
# 用通配前缀而不是逐个列 —— 后面加新的自检库时最容易忘记补这一行。
_selftest*.db
_selftest*.db-*

# ── 模型文件：首次运行自动下载（约 11MB）──
models/

# ── Python ──
.venv/
__pycache__/
*.py[cod]
# pip install . / -e . 会在仓库根生成，属于构建产物
*.egg-info/

# ── PyInstaller 构建产物（exe 发布走 GitHub Release，不进仓库）──
build/
dist/
*.spec

# ── 编辑器 / 系统 ──
.vscode/
.idea/
Thumbs.db
desktop.ini
